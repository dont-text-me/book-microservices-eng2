jpa:
  default:
    properties:
      hibernate:
        hbm2ddl:
          auto: create-drop
books:
  url: /books

kafka:
  enabled: false
  embedded:
    enabled: true
    topics:
      - book-read
      - book-read-by-day
  streams:
    default:
      start-kafka-streams: true